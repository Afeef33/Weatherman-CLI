# Weatherman CLI Tool 🌦️

A Python-based command-line tool to analyze weather data.

## Features
- **Yearly Report:** Get highest/lowest temperatures (`-e`).
- **Monthly Report:** Get average stats (`-a`).
- **Charts:** Visual bar charts for temperatures (`-c`).

## Usage
```bash
python weatherman.py weather_data -e 2004